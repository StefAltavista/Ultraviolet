export declare type DeepNullable<T> = {
    [K in keyof T]: DeepNullable<T[K]> | null;
};
export declare type ListenerOptions = boolean | {
    capture?: boolean;
    once?: boolean;
    passive?: boolean;
    signal?: AbortSignal;
};
export declare type ExcludeFunction<T> = Exclude<T, Function>;
